type Craft @entity {
  id: ID!
  recipe: Recipe!
  count: BigInt!
  user: Bytes! # address
}

type Recipe @entity {
  id: ID!
  count: BigInt!
  ingredients: [BigInt!]!
  ingredientQuantities: [BigInt!]!
  products: [BigInt!]!
  productQuantities: [BigInt!]!
  disabled: Boolean!
}
